# Environment Variables for Hugging Face Spaces
# Copy this to .env if you need to configure paths

# Data directory (default: ./data)
DATA_DIR=./data

# Database path
DB_PATH=./data/vibecheck.db

# Image directory
IMAGE_DIR=./data/images

# FAISS index path
FAISS_PATH=./data/vibecheck_index.faiss

# Metadata path
META_PATH=./data/meta_ids.npy

# Model cache (optional - helps with model download timeouts)
TRANSFORMERS_CACHE=/tmp/models

# Device (auto-detected, but can override)
# DEVICE=cuda  # Use GPU if available
# DEVICE=cpu   # Force CPU
